import { Meta } from '@storybook/blocks'

<Meta title="Changelog" />

<style>{`
  .ds-changelog {
    max-width: 720px;
    margin: 0 auto;
    padding: 48px 24px;
    font-family: var(--text-family-static-body);
  }
  .ds-changelog h1 {
    font-size: var(--text-size-static-display3);
    font-family: var(--text-family-static-headline);
    color: var(--text-color-themeable-primary);
    margin: 0 0 8px;
  }
  .ds-changelog .subtitle {
    font-size: var(--text-size-static-body2);
    color: var(--text-color-themeable-secondary);
    margin: 0 0 48px;
  }
  .ds-changelog hr {
    border: none;
    border-top: 1px solid var(--border-color-themeable-primary);
    margin: 32px 0;
  }
  .ds-changelog .version-header {
    display: flex;
    align-items: baseline;
    gap: 12px;
    margin-bottom: 16px;
  }
  .ds-changelog h2 {
    font-size: var(--text-size-static-headline1);
    color: var(--text-color-themeable-primary);
    margin: 0;
  }
  .ds-changelog .date {
    font-size: var(--text-size-static-caption);
    color: var(--text-color-themeable-tertiary);
  }
  .ds-changelog h4 {
    font-size: var(--text-size-static-body2);
    font-weight: var(--text-weight-static-semibold);
    color: var(--text-color-static-brand-primary);
    margin: 16px 0 6px;
  }
  .ds-changelog ul {
    margin: 0 0 8px;
    padding-left: 20px;
  }
  .ds-changelog li {
    font-size: var(--text-size-static-body2);
    color: var(--text-color-themeable-secondary);
    line-height: 1.6;
    margin-bottom: 4px;
  }
  .ds-changelog code {
    font-family: var(--text-family-static-mono);
    font-size: var(--text-size-static-caption);
    background: var(--container-color-themeable-secondary);
    border: 1px solid var(--border-color-themeable-primary);
    border-radius: 4px;
    padding: 1px 5px;
    color: var(--text-color-themeable-primary);
  }
`}</style>

<div className="ds-changelog">

<h1>Changelog</h1>
<p className="subtitle">All notable changes to the Base Design System.</p>

<hr />

<div className="version-header">
  <h2>0.1.5</h2>
  <span className="date">2026-06-06</span>
</div>

<h4>Added</h4>
<ul>
  <li>Storybook Welcome page with install guide, setup instructions, and component overview</li>
  <li>Storybook Changelog page</li>
  <li><code>CHANGELOG.md</code> at repo root</li>
</ul>

<hr />

<div className="version-header">
  <h2>0.1.4</h2>
  <span className="date">2026-06-06</span>
</div>

<h4>Added</h4>
<ul>
  <li><code>llms.txt</code> — full component API reference for AI coding agents</li>
  <li><code>CLAUDE.md</code> — Claude-specific project conventions and styling guide</li>
</ul>

<hr />

<div className="version-header">
  <h2>0.1.3 — 0.1.2</h2>
  <span className="date">2026-06-06</span>
</div>

<h4>Fixed</h4>
<ul>
  <li>Added <code>./dist/index</code> and <code>./dist/index.js</code> to the package exports map for Figma Make CDN compatibility</li>
</ul>

<hr />

<div className="version-header">
  <h2>0.1.1</h2>
  <span className="date">2026-06-06</span>
</div>

<h4>Added</h4>
<ul>
  <li>GitHub Actions CI/CD — pushing a version tag auto-publishes to the Figma npm registry</li>
  <li>Full <code>README.md</code> with install, setup, theming, component table, and token docs</li>
  <li><code>sideEffects</code> field so bundlers preserve the token CSS</li>
</ul>

<h4>Fixed</h4>
<ul>
  <li>6 missing barrel exports — <code>Alert</code>, <code>Avatar</code>, <code>Badge</code>, <code>Checkbox</code>, <code>DataGrid</code>, <code>UserIdentificationTag</code></li>
  <li>FontAwesome peer dependency range corrected from <code>>=6</code> to <code>>=7</code></li>
  <li>JetBrains Mono added to Google Fonts import</li>
  <li>Font family names corrected in token JSON (Poppins → DM Serif Display, Inter → DM Sans)</li>
  <li><code>ListItem</code> subtitle prop widened to <code>React.ReactNode</code></li>
</ul>

<h4>Changed</h4>
<ul>
  <li>Version bumped from <code>0.0.1</code> to <code>0.1.0</code></li>
</ul>

<hr />

<div className="version-header">
  <h2>0.0.1</h2>
  <span className="date">2026-05-01</span>
</div>

<h4>Added</h4>
<ul>
  <li>Initial release with 60+ components</li>
  <li>CSS custom property design token system with dark/light theming</li>
  <li>Full TypeScript support with exported prop types</li>
  <li>Storybook with theme switcher, a11y addon, and docs</li>
  <li>Vite library build outputting ESM and CJS formats</li>
</ul>

</div>
